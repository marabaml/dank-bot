[build]
builder = "nixpacks"

[deploy]
startCommand = "cd artifacts/api-server && pnpm run build && pnpm run start"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
